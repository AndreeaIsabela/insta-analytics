div
  .grid-container.card-group(v-if="store.state.photos.length")
    .grid-item.mb-3(v-for="(photo, index) in store.state.photos" :key="index")
      .card
        img.photo.card-img-top(:src="photo.media_url")
        .card-body
          p.card-text(v-if="photo.caption")
            .font-weight-bold {{photo.username}}: 
            span.caption {{photo.caption}}
          p.card-text
            small.text-muted Posted {{time(photo.timestamp)}}
  .row(v-else)
    .col-12
      p.text-center.noContent
        img.img-fluid(src="https://es.seaicons.com/wp-content/uploads/2015/11/note-icon.png")
        h6.font-weight-bold.text-center You don't have any photo